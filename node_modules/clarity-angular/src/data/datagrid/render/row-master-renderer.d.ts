import { AfterViewInit, OnDestroy, TemplateRef, ViewContainerRef } from "@angular/core";
import { DatagridRenderOrganizer } from "./render-organizer";
/**
 * This component serves as a conditional wrapper.
 * When in table mode, it puts its content next to itself rather than inside.
 */
export declare class DatagridRowMasterRenderer implements AfterViewInit, OnDestroy {
    private outsideContainer;
    private organizer;
    constructor(outsideContainer: ViewContainerRef, organizer: DatagridRenderOrganizer);
    private subscription;
    ngOnDestroy(): void;
    insideContainer: ViewContainerRef;
    projected: TemplateRef<any>;
    private outside;
    ngAfterViewInit(): void;
    projectOutside(outside: boolean): void;
}
